# =============================================================================
# iMedizin monorepo – environment variables (reference only)
# =============================================================================
# Use per-app .env files. Copy the examples into each app:
#   apps/api/.env.example      → apps/api/.env
#   apps/dashboard/.env.example → apps/dashboard/.env
# =============================================================================

# -----------------------------------------------------------------------------
# API (apps/api)
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:password@localhost:5432/imedizin
# REDIS_HOST=localhost
# REDIS_PORT=6379
# PORT=3000
# FRONTEND_URL=http://localhost:8080
# ATTACHMENTS_PATH=./data/attachments
# MS_TENANT_ID=
# MS_CLIENT_ID=
# MS_CLIENT_SECRET=
# MAILBOX_EMAIL=support@imedizin.com
# WEBHOOK_BASE_URL=

# -----------------------------------------------------------------------------
# Dashboard (apps/dashboard) – Vite exposes only VITE_* to the client
# -----------------------------------------------------------------------------
# VITE_API_BASE_URL=   # empty in dev = use Vite proxy (/api → API on :3000)
