# Dashboard (Vite/React) – copy to .env in this directory (apps/dashboard)
# Vite only exposes variables prefixed with VITE_ to the client.

# API base URL (no trailing slash).
# - Leave empty or unset in dev to use Vite proxy: requests to /api go to API on port 3000.
# - Set for production or when API is on another host, e.g. https://api.example.com
VITE_API_BASE_URL=
